# Artifacts

Artifacts are intentionally explicit. Raw validation transcripts stay in local/external HK state and are referenced from `README.md`; attach durable, reviewable files with `hk artifact attach` before export when they should be shared.
